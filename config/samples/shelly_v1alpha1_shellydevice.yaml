# ShellyDevice objects are normally created by the discovery sweeper and
# named by lowercased MAC. Users only set spec fields, e.g. to pause
# management or pin a profile.
apiVersion: shelly.thirdimpact.io/v1alpha1
kind: ShellyDevice
metadata:
  name: 3c8a1fec8e3c
spec:
  displayName: office-desk
  paused: false
